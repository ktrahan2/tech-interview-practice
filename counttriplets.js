//given an array
//given an value for r, where r is the number to multiple be to get the next, "common ratio"

//count the number of triplets that match the geometric progression
//do a loop, then create an object that holds all the numbers for that triple
//increment them as you find them. so for the first loop you get
// {1: 1, 2: 2, 4: 1}
//{2: 2, 4: 1}
//{2:1, 4: 1}
//grab the highest number from the values?

const countTriplets = (array, r) => {
    
    let count = 0
    let tripletObject = {}

    

    return tripletObject
}

let r = 2
let array = [1, 2, 2, 4, 3]

console.log(countTriplets(array, r))